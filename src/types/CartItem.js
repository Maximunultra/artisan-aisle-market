
// This file now contains JS comments instead of TypeScript interfaces

/**
 * @typedef {Object} CartItem
 * @property {number} id
 * @property {string} name
 * @property {number} price
 * @property {string} image
 * @property {string} artisan
 * @property {number} quantity
 */

// No exports needed for the CartItem interface since it's now a JSDoc comment
